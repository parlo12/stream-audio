# Deployed to the host at /etc/nginx/conf.d/ratelimit.conf
# (conf.d/*.conf is included in the nginx http{} context).
#
# Per-IP rate limiting for brute-force-sensitive auth endpoints (Phase 5).
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/m;
limit_req_status 429;
