.DS_Store
# secrets
.env
.env*
!.env.example

# local backups and logs
*.backup
logs/
.DS_Store
# compiled binaries
auth-service/auth-service
gateway/gateway.git
content-service/content-service

# Deploy secrets mounted into containers (APNs .p8, etc.) — never commit
secrets/
